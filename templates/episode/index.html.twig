{% extends 'base.html.twig' %}

{% block title %}Episode index{% endblock %}

{% block body %}
    <h1>Episode index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Program</th>
                <th>Season</th>
                <th>Title</th>
                <th>Number</th>
                <th>Synopsis</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for episode in episodes %}

            <tr>
                <td>{{ program }}</td>
                <td>{{ number }}</td>
                <td>{{ episode.title }}</td>
                <td>{{ episode.number }}</td>
                <td>{{ episode.synopsis }}</td>
                <td>
                    <a href="{{ path('episode_show', {'sluge': episode.sluge, 'slug' : slug, 'number' : number }) }}">show</a>
                    <a href="{{ path('episode_edit', {'sluge': episode.sluge, 'slug' : slug, 'number' : number }) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('episode_new', {'slug' : slug, 'number' : number }) }}">Create new</a>
{% endblock %}
